# 🛒 Система управления продуктами и категориями

Простая и эффективная система для управления товарами и их категориями в интернет-магазине.

## 📌 Описание

Этот проект реализует базовую функциональность для управления продуктами и категориями в интернет-магазине. Система позволяет создавать различные типы товаров, группировать их по категориям и выполнять основные операции.

## ✨ Возможности

- Создание и управление продуктами разных типов
- Группировка товаров по категориям
- Наследование для создания специализированных товаров
- Проверка типов при операциях с товарами
- Автоматическое логирование создания объектов
- Полное тестирование с отчетами о покрытии

## 📁 Структура проекта

```
project/
├── src/                    # Исходный код
│   ├── __init__.py
│   ├── base_product.py    # Абстрактный класс
│   ├── product.py         # Базовый класс Product
│   ├── smartphone.py      # Смартфоны
│   ├── lawngrass.py       # Газонная трава
│   ├── category.py        # Категории
│   └── log_mixin.py       # Миксин для логирования
├── tests/                 # Тесты
├── reports/               # Отчеты тестирования
├── requirements.txt       # Зависимости
├── run_tests.py          # Скрипт запуска тестов
└── README.md             # Этот файл
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Пример использования

```python
from src.product import Product
from src.smartphone import Smartphone
from src.lawngrass import LawnGrass
from src.category import Category

# Создаем продукты
iphone = Smartphone(
    name="iPhone 15",
    description="Смартфон Apple",
    price=89990,
    quantity=50,
    efficiency="A16 Bionic",
    model="15 Pro",
    memory=256,
    color="Титановый"
)

grass = LawnGrass(
    name="Газонная трава",
    description="Трава для газона",
    price=2500,
    quantity=200,
    country="Россия",
    germination_period=14,
    color="Зеленый"
)

# Создаем категории
phones_category = Category("Смартфоны", "Мобильные устройства")
garden_category = Category("Сад", "Садовые товары")

# Добавляем товары в категории
phones_category.add_product(iphone)
garden_category.add_product(grass)

# Выводим информацию
print(phones_category)
print(garden_category)
```

### 3. Запуск тестов

```bash
# Простой запуск
python run_tests.py

# С детальным отчетом
pytest tests/ -v

# С отчетом о покрытии кода
pytest tests/ --cov=src --cov-report=html
```

## 🧩 Основные классы

### 1. Product (Базовый класс товара)
- `name` - название товара
- `description` - описание
- `price` - цена
- `quantity` - количество на складе

### 2. Smartphone (Смартфоны)
Наследует Product + дополнительные поля:
- `efficiency` - производительность
- `model` - модель
- `memory` - объем памяти
- `color` - цвет

### 3. LawnGrass (Газонная трава)
Наследует Product + дополнительные поля:
- `country` - страна-производитель
- `germination_period` - срок прорастания
- `color` - цвет

### 4. Category (Категория)
- `name` - название категории
- `description` - описание категории
- `add_product()` - добавить товар в категорию

## 📊 Тестирование

Проект имеет полное покрытие тестами:

```bash
# Запуск всех тестов
pytest tests/

# Тесты с отчетом о покрытии
pytest tests/ --cov=src --cov-report=term-missing

# Генерация HTML отчета
pytest tests/ --html=report.html --self-contained-html
```

Отчеты тестирования сохраняются в папке `reports/`.

## 🔧 Требования

- Python 3.8+
- pytest
- pytest-cov
- pytest-html

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Добавьте тесты
4. Запустите тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License. Смотрите файл LICENSE для подробностей.

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.